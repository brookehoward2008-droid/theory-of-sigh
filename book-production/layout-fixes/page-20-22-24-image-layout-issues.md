# Page 20, 22, and 24 Image Layout Issues

Source PDF reviewed:

`eyes_layout_preserved_author_credit_no_pink_guides_52pg.pdf`

Pages rendered for visual check:

- 20
- 22
- 24

## Brooke feedback

Pages 20, 22, and 24 have image layout issues and should not be treated as final print pages.

## Visual review notes

### Page 20

Current issue: collage-style image stack looks unresolved. The images overlap in a way that makes the left spread feel accidental rather than intentionally composed. The lower large image sits heavily against the image stack and the caption block is very close to the bottom edge of the image.

Suggested correction:

- Rebuild this page as a cleaner constraint-section opener.
- Keep the existing text block on the right.
- Use either one dominant image with one supporting image, or a controlled two-image overlap with consistent margins.
- Keep the caption inside a clean bottom band or remove it from the visual collision area.

### Page 22

Current issue: the three-image horizontal strip is cleaner than page 20, but it needs verification against the image sequence logic. The first caption overlay is heavy and may not align with the intended image identity/source register.

Suggested correction:

- Confirm the three images belong on this page.
- Align all three image boxes to the same top and bottom crop line.
- Verify the caption belongs to the first image only.
- Keep the sequence label below the strip if this remains a sequence page.

### Page 24

Current issue: page 24 repeats the horizontal triptych behavior and appears compositionally unfinished, with a large unused lower field. It may be a duplicate or misordered image sequence page.

Suggested correction:

- Confirm whether page 24 should be a full-image page, a three-image sequence, or a text/image page.
- If it remains a sequence page, rebuild the strip with deliberate spacing and consistent caption placement.
- If it is a mistaken duplicate/reuse, replace with the intended image from the source register.

## Production rule

Do not fix these by accepting a full layout rebuild. Apply a surgical page-level layout correction only after the intended images and page roles are verified.

Preserve:

- 52-page count
- author credit
- removed pink guides
- current typography fixes
- no broad PR #6 merge

## Next action

Create a page-level correction pass for pages 20, 22, and 24 only, then re-render those pages and compare before/after.
